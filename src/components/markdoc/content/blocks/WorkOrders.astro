---
import BuildingContentBlock from '@components/markdoc/content/BuildingContentBlock.astro';
import { getBuildingData, getBuildingName } from '@utils/building';

import type { BaseContentBlockProps } from '../types';

type Props = BaseContentBlockProps;

const { name } = Astro.props;

const buildingData = await getBuildingData(name);

const content = `This page shows you what work orders have been assigned to this ${getBuildingName(
  buildingData,
  false
)}, along with the distance away from the building. You can assign or cancel work orders here.`;
---

<BuildingContentBlock
  {...Astro.props}
  header="workorders"
  imageKey="workorders"
  imageAlt="Work orders"
  markdownContent={content}
/>
