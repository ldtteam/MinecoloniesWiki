---
import { getCollection } from 'astro:content';

import PackList from './PackList.astro';

const officialCitizenNamePacks = await getCollection('citizen_name_packs', (pack) => pack.data.official === true);
---

<PackList packs={officialCitizenNamePacks} />
