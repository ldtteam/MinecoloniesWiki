---
import { getCollection } from 'astro:content';

import PackList from './PackList.astro';

const citizenNamePacks = await getCollection('citizen_name_packs', (pack) => pack.data.official === false);
---

<PackList packs={citizenNamePacks} />
