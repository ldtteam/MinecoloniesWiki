---
import ItemInfoboxInternal from '@components/markdoc/infobox/ItemInfoboxCommon.astro';
import Wrapper from '@components/ui/infobox/Wrapper.astro';

interface Props {
  item: string;
}

const { item } = Astro.props;
---

<Wrapper>
  <slot />

  <ItemInfoboxInternal slot="infobox" item={item} />
</Wrapper>
