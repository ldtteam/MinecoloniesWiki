---
import { getCollection } from 'astro:content';

import ResearchListTree from './ResearchListTree.astro';

const trees = await getCollection('research_tree');
---

{trees.map((tree) => <ResearchListTree tree={tree} />)}
