---
import WikiNavigation from '@components/navbar/WikiNavigation.astro';
import Layout from '@layouts/Default.astro';
import { getEntry } from 'astro:content';

import { getWikiTitle } from '../../util/wiki';

const entry = await getEntry('wiki', 'index');
const title = await getWikiTitle(entry);
const { Content } = await entry.render();
---

<Layout
  title={title}
  titleFormat={(title) => `MineColonies Wiki | ${title}`}
  header="Welcome to the MineColonies Wiki!"
>
  <WikiNavigation slot="after-nav" />

  <Content frontmatter={entry.data} />

  <hr />
  <p class="text-center">
    If changes are needed or you think there is content missing, feel free to
    <a href="https://github.com/ldtteam/MinecoloniesWiki/edit/main/src/content/wiki/index.mdoc">edit</a>
     this page (the button at the top right) or submit an
    <a href="https://github.com/ldtteam/MinecoloniesWiki/issues/new/choose"> issue</a>
     for us to make edits. - MineColonies Wiki Team
  </p>
</Layout>
