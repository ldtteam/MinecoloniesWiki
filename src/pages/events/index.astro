---
import { getCollection } from 'astro:content';

import EventList from '../../components/EventList.svelte';
import Layout from '../../layouts/Default.astro';

const eventsRaw = await getCollection('events');
---

<Layout title="Events">
  <EventList events={eventsRaw} />
</Layout>
